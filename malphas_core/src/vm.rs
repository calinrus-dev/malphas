// The bytecode sandbox VM was deprecated in Malphas v2.7.0.
//
// Logic is now provided by hot-swappable `.mxc` dynamic libraries that receive
// the Silver Platter lookup table directly on every tick.  This module is kept
// as a placeholder so historical references do not break during the migration.
