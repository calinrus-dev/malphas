name: Flutter CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  rust-core:
    uses: ./.github/workflows/rust_ci.yml

  analyze-test:
    name: Flutter analyze and test
    needs: rust-core
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: flutter_app
    steps:
      - uses: actions/checkout@v4

      - name: Download native artifact
        uses: actions/download-artifact@v4
        with:
          name: malphas_core-ubuntu-latest
          path: native/

      - name: Place native library where MalphasBindings expects it
        shell: bash
        run: |
          if [ -f native/libmalphas_core.so ]; then
            cp native/libmalphas_core.so ../libmalphas_core.so
          elif [ -f native/malphas_core.dll ]; then
            cp native/malphas_core.dll ../malphas_core.dll
          elif [ -f native/libmalphas_core.dylib ]; then
            cp native/libmalphas_core.dylib ../libmalphas_core.dylib
          fi

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Analyze
        run: flutter analyze --no-fatal-infos --no-fatal-warnings

      - name: Run tests (headless)
        run: flutter test
