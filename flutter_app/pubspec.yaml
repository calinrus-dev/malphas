name: malphas_app
description: "Malphas v2.10.0 — Sovereign Runtime"
publish_to: 'none'
version: 2.10.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  ffi: ^2.1.2
  path_provider: ^2.1.1
  path: ^1.9.0
  flutter_secure_storage: ^9.2.2
  crypto: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  fonts:
    - family: JetBrainsMono
      fonts:
        - asset: assets/fonts/JetBrainsMono-Regular.ttf

  # Asset section declared for future engine asset integration
  assets:
    - assets/fonts/
    - assets/sprites/
    - assets/packages/
    - assets/trust_anchor.pem
