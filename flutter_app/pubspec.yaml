name: malphas_app
description: "Consola Virtual Agnostica Malphas v1.0"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  ffi: ^2.1.2
  archive: ^3.3.7
  path_provider: ^2.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  # Sección de recursos declarada para futura integración de assets del motor
  assets:
    - assets/fonts/
    - assets/sprites/
