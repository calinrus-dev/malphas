name: malphas_app
description: "Malphas v2.4.0 — Decoupled & Automated Onboarding"
publish_to: 'none'
version: 2.4.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  ffi: ^2.1.2
  archive: ^3.3.7
  path_provider: ^2.1.1
  crypto: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  fonts:
    - family: JetBrainsMono
      fonts:
        - asset: assets/fonts/JetBrainsMono-Regular.ttf

  # Sección de recursos declarada para futura integración de assets del motor
  assets:
    - assets/fonts/
    - assets/sprites/
