Guía rápida para contribuidores y mantenedores.

1. Ejecuta `scripts\clean_repo.ps1` para limpiar artefactos.
2. Para el desarrollo Flutter: `cd flutter_app && flutter pub get && flutter run -d <device>`
3. Para el desarrollo Rust: `cd malphas_core && cargo build --release`
